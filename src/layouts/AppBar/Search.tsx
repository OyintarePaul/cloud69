import { Input } from "../../components/ui/input";

const Search = () => {
  return (
    <Input
      type="search"
      placeholder="Search"
      className="w-full max-w-md bg-muted"
    />
  );
};
export default Search;
